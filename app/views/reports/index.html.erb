<h1>Listing reports</h1>
 
<%= link_to 'New report', new_report_path %>
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @reports.each do |report| %>
    <tr>
      <td><%= report.title %></td>
      <td><%= report.text %></td>
      <td><%= link_to 'Show', report_path(report) %></td>
    </tr>
  <% end %>
</table>